services:
  centurion:
    image: nginx:alpine
    volumes:
      - ./src/:/usr/share/nginx/html/
      - ./media/:/usr/share/nginx/html/media
    ports:
      - "8080:80"
    restart: unless-stopped
